# Implementation Plan: Database Query Tool

**Branch**: `001-db-query-tool` | **Date**: 2026-01-15 | **Spec**: [spec.md](./spec.md)
**Input**: Feature specification from `/specs/001-db-query-tool/spec.md`

**Note**: This plan is generated by the `/speckit.plan` command following the workflow defined in `.specify/templates/commands/plan.md`.

## Summary

The Database Query Tool is a web application that enables users to connect to databases (primarily PostgreSQL), explore database structure through metadata visualization, execute SQL queries with safety constraints (SELECT-only, automatic LIMIT), and generate SQL from natural language descriptions. Query results can be exported to CSV, Excel, or JSON formats. The tool stores database connections and metadata locally in SQLite for quick retrieval, and uses LLM integration for natural language to SQL conversion.

## Technical Context

**Language/Version**: Python 3.13+ (backend), TypeScript with strict mode (frontend)
**Primary Dependencies**: 
- Backend: FastAPI, Pydantic, sqlglot, OpenAI SDK, SQLAlchemy, asyncpg
- Frontend: React 19+, Refine 5, Tailwind CSS 4, Ant Design 5, Monaco Editor

**Storage**: SQLite database at `~/.dataforge.db` for storing database connections and metadata cache
**Testing**: pytest (backend), Vitest + React Testing Library (frontend)
**Target Platform**: Web application - modern browsers (Chrome, Firefox, Safari, Edge)
**Project Type**: Web application (separate backend/frontend)
**Performance Goals**:
- Database connection and metadata retrieval: <10 seconds for 100 tables
- Query execution and result display: <5 seconds for 1000 rows
- Export generation: <3 seconds for 1000 rows
- Previously connected database load: <1 second

**Constraints**:
- SELECT-only queries enforced via sqlglot parsing
- Automatic LIMIT 1000 added to unbounded queries
- CORS enabled for all origins (allow_origins=["*"])
- OpenAI API key from OPENAI_API_KEY environment variable
- No authentication required (open access)

**Scale/Scope**:
- Support databases with up to 500 tables
- Handle up to 1000 rows in result sets (with LIMIT enforcement)
- Support multiple concurrent database connections
- Local metadata storage scales with number of databases added

## Constitution Check

*GATE: Must pass before Phase 0 research. Re-check after Phase 1 design.*

### I. Language & Style Standards ✅

- **Backend**: Python with ergonomic, Pythonic style - COMPLIANT
- **Frontend**: TypeScript with strict mode enabled - COMPLIANT
- **Evidence**: Constitution mandates Python backend and TypeScript frontend; this plan uses both

### II. Type Safety (NON-NEGOTIABLE) ✅

- **Backend Type Hints**: All Pydantic models, FastAPI endpoints, and service functions will use comprehensive type annotations - COMPLIANT
- **Frontend TypeScript**: `strict: true` in tsconfig.json, avoid `any` type - COMPLIANT
- **Evidence**: Plan includes Pydantic models for all entities, TypeScript types generated from OpenAPI schema

### III. Data Modeling Standards ✅

- **Pydantic Models**: All data entities (DatabaseConnection, DatabaseMetadata, QueryResult, ExportOperation) will be defined as Pydantic models - COMPLIANT
- **Validation**: Runtime validation through Pydantic, OpenAPI schema generation - COMPLIANT
- **Evidence**: data-model.md will define all entities as Pydantic models with Field descriptions

### IV. API Contract Standards ✅

- **camelCase Output**: All Pydantic models will use `alias_generator=to_camel` with `populate_by_name=True` - COMPLIANT
- **Internal snake_case**: Python backend code uses snake_case, exports camelCase JSON - COMPLIANT
- **Evidence**: API contracts in `/contracts/` will show camelCase field names matching frontend TypeScript interfaces

### V. Accessibility & Security ✅

- **No Authentication**: Application accessible to all users without login - COMPLIANT
- **Input Validation**: sqlglot parsing for SQL validation, URL validation for connections - COMPLIANT
- **Query Restrictions**: SELECT-only enforcement, automatic LIMIT clauses - COMPLIANT
- **Evidence**: Security through validation rather than authentication, per constitution requirements

### Technical Stack Compliance ✅

- **Backend**: Python (uv), FastAPI, Pydantic, sqlglot, OpenAI SDK, SQLite - COMPLIANT
- **Frontend**: React, Refine 5, Tailwind CSS, Ant Design, Monaco Editor - COMPLIANT
- **Evidence**: All stack choices match constitution Technical Stack Standards section

**GATE STATUS**: ✅ PASSED - All constitutional principles satisfied. No violations to justify.

## Project Structure

### Documentation (this feature)

```text
specs/001-db-query-tool/
├── plan.md              # This file (/speckit.plan command output)
├── research.md          # Phase 0 output (/speckit.plan command)
├── data-model.md        # Phase 1 output (/speckit.plan command)
├── quickstart.md        # Phase 1 output (/speckit.plan command)
├── contracts/           # Phase 1 output (/speckit.plan command)
│   └── openapi.yaml     # OpenAPI 3.0 specification
└── checklists/
    └── requirements.md  # Quality checklist (already created)
```

### Source Code (repository root)

```text
backend/
├── src/
│   ├── main.py                    # FastAPI application entry point
│   ├── config.py                  # Environment configuration (OPENAI_API_KEY, DB path)
│   ├── models/
│   │   ├── __init__.py
│   │   ├── database.py            # DatabaseConnection, DatabaseMetadata Pydantic models
│   │   ├── query.py               # QueryRequest, QueryResult, NaturalLanguageRequest
│   │   └── export.py              # ExportRequest, ExportOperation models
│   ├── services/
│   │   ├── __init__.py
│   │   ├── db_connection.py       # Database connection and metadata retrieval
│   │   ├── metadata_cache.py      # SQLite metadata storage operations
│   │   ├── query_executor.py      # SQL validation, execution, result formatting
│   │   ├── query_parser.py        # sqlglot-based SQL parsing and validation
│   │   ├── nl_to_sql.py           # Natural language to SQL using OpenAI
│   │   └── export_service.py      # CSV/Excel/JSON export generation
│   ├── api/
│   │   ├── __init__.py
│   │   ├── v1/
│   │   │   ├── __init__.py
│   │   │   ├── databases.py       # GET /dbs/, PUT /dbs/{name}, GET /dbs/{name}
│   │   │   ├── queries.py         # POST /dbs/{name}/query
│   │   │   ├── natural.py         # POST /dbs/{name}/query/natural
│   │   │   └── exports.py         # POST /dbs/{name}/export
│   └── db/
│       ├── __init__.py
│       └── sqlite_manager.py      # SQLite database initialization and operations
├── tests/
│   ├── contract/
│   │   └── test_api_contracts.py  # OpenAPI contract validation
│   ├── integration/
│   │   ├── test_db_connection.py  # Database connection integration tests
│   │   ├── test_query_flow.py     # End-to-end query execution tests
│   │   └── test_export.py         # Export format generation tests
│   └── unit/
│       ├── test_query_parser.py   # SQL parsing and validation tests
│       └── test_nl_to_sql.py      # Natural language conversion tests
├── pyproject.toml                 # uv project configuration
└── README.md                      # Backend setup and development instructions

frontend/
├── src/
│   ├── App.tsx                    # Main application component
│   ├── main.tsx                   # Entry point, Refine provider setup
│   ├── types/
│   │   └── api.ts                 # TypeScript types generated from OpenAPI
│   ├── components/
│   │   ├── DatabaseList.tsx       # List of added databases
│   │   ├── DatabaseMetadata.tsx   # Table/view metadata display
│   │   ├── SQLEditor.tsx          # Monaco Editor wrapper for SQL input
│   │   ├── QueryResults.tsx       # Table display for query results
│   │   ├── ExportButton.tsx       # Export dropdown (CSV/Excel/JSON)
│   │   └── NaturalLanguageInput.tsx # Natural language query input
│   ├── pages/
│   │   ├── databases/
│   │   │   ├── list.tsx           # Database list page (Refine resource)
│   │   │   ├── create.tsx         # Add database form
│   │   │   └── show.tsx           # Database detail with query interface
│   │   └── query/
│   │       └── execute.tsx        # Query execution page
│   ├── services/
│   │   ├── api.ts                 # Axios instance with base URL configuration
│   │   └── dataProvider.ts        # Refine data provider for API integration
│   └── styles/
│       └── index.css              # Tailwind CSS imports and custom styles
├── tests/
│   ├── components/
│   │   └── SQLEditor.test.tsx     # Component unit tests
│   └── integration/
│       └── queryFlow.test.tsx     # User flow integration tests
├── package.json                   # npm dependencies
├── tsconfig.json                  # TypeScript configuration (strict: true)
├── tailwind.config.js             # Tailwind CSS configuration
├── vite.config.ts                 # Vite build configuration
└── README.md                      # Frontend setup and development instructions

shared/
└── README.md                      # Documentation for shared concerns (CORS, API versioning)
```

**Structure Decision**: Web application structure (Option 2) selected because the feature requires both a backend API (FastAPI) and a frontend UI (React). The backend handles database connections, SQL execution, and LLM integration, while the frontend provides the user interface with SQL editor, metadata display, and result visualization.

The separation follows the constitution's language standards (Python backend, TypeScript frontend) and enables independent development and deployment of each layer. The `shared/` directory documents cross-cutting concerns like CORS configuration and API versioning.

## Complexity Tracking

> **No violations** - Constitution Check passed all gates. This section intentionally left minimal.

All architectural decisions align with constitutional principles. No complexity justification required.

---

## Phase 0: Research (Completed)

**Status**: ✅ Complete
**Output**: [research.md](./research.md)

**Key Decisions**:
- PostgreSQL metadata via information_schema and pg_catalog
- sqlglot for SQL parsing, validation, and AST manipulation
- OpenAI GPT-4 for natural language to SQL generation
- FastAPI with async endpoints and dependency injection
- Specialized export libraries (csv, openpyxl, json)
- Refine for frontend state management and data provider
- Monaco Editor with SQL language support
- SQLite with JSON columns for metadata cache

All technical unknowns resolved. No NEEDS CLARIFICATION markers remain.

---

## Phase 1: Design & Contracts (Completed)

**Status**: ✅ Complete
**Outputs**:
- [data-model.md](./data-model.md) - All Pydantic models defined
- [contracts/openapi.yaml](./contracts/openapi.yaml) - Complete OpenAPI 3.0 specification
- [quickstart.md](./quickstart.md) - Development setup guide
- `.cursor/rules/specify-rules.mdc` - Updated agent context

**Data Models Created**:
- DatabaseConnection, DatabaseMetadata, TableMetadata, ColumnMetadata
- QueryRequest, QueryResult
- NaturalLanguageRequest, NaturalLanguageResponse
- ExportRequest, ExportResponse, ExportFormat
- ErrorResponse, ErrorDetail
- AppConfig (environment configuration)

**API Endpoints Defined**:
- GET /api/v1/dbs/ - List all databases
- PUT /api/v1/dbs/{name} - Add/update database connection
- GET /api/v1/dbs/{name} - Get database details with metadata
- POST /api/v1/dbs/{name}/query - Execute SQL query
- POST /api/v1/dbs/{name}/query/natural - Generate SQL from natural language
- POST /api/v1/dbs/{name}/export - Export query results

**Constitution Re-Check (Post-Design)**:

### I. Language & Style Standards ✅
- Backend models use Pythonic snake_case internally - COMPLIANT
- Pydantic `alias_generator=to_camel` for JSON output - COMPLIANT
- Frontend TypeScript types generated from OpenAPI - COMPLIANT

### II. Type Safety ✅
- All Pydantic models have comprehensive Field annotations - COMPLIANT
- OpenAPI schema includes types for frontend TypeScript generation - COMPLIANT
- mypy --strict validation planned - COMPLIANT

### III. Data Modeling Standards ✅
- All entities defined as Pydantic models - COMPLIANT
- Runtime validation via Pydantic - COMPLIANT
- OpenAPI schema auto-generated - COMPLIANT

### IV. API Contract Standards ✅
- All response examples show camelCase field names - COMPLIANT
- Internal snake_case preserved in Python code - COMPLIANT
- OpenAPI spec documents camelCase API surface - COMPLIANT

### V. Accessibility & Security ✅
- No authentication in OpenAPI spec (security: []) - COMPLIANT
- SQL validation via sqlglot documented - COMPLIANT
- SELECT-only enforcement in API descriptions - COMPLIANT

**GATE STATUS**: ✅ PASSED - Design maintains full constitutional compliance

---

## Implementation Readiness

**Phase 2 Status**: Ready for `/speckit.tasks` command

All prerequisites complete:
- ✅ Constitution compliance verified (pre and post design)
- ✅ Technical research resolved all unknowns
- ✅ Data models fully specified with validation rules
- ✅ API contracts documented in OpenAPI format
- ✅ Development workflow defined in quickstart guide
- ✅ Agent context updated with project technologies

**Next Command**: `/speckit.tasks` to generate implementation task breakdown

**Estimated Implementation**:
- Phase 1 (P1 - Database Connection): ~5-7 days
- Phase 2 (P2 - Manual SQL Queries): ~5-7 days
- Phase 3 (P3 - Natural Language SQL): ~3-5 days
- Phase 4 (P4 - Export Results): ~2-3 days
- Polish & Testing: ~3-5 days
- **Total**: ~18-27 days for complete implementation

**Risk Assessment**: LOW
- All dependencies are well-established libraries
- Clear architectural boundaries (backend/frontend separation)
- Progressive enhancement via prioritized user stories
- Each story independently testable and deployable
